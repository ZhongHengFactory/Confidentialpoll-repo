# ConfidentialPoll (FHEVM demo)

This repository contains a Solidity contract and a React + Vite frontend demo that demonstrates confidential voting using FHE primitives.

## Quick local (frontend)
cd frontend
npm install
# set VITE_CONTRACT env variable to your deployed contract address
npm run dev
